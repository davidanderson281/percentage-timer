import { RemoveTwelveFromTimePipe } from './remove-twelve-from-time.pipe';

describe('RemoveTwelveFromTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveTwelveFromTimePipe();
    expect(pipe).toBeTruthy();
  });
});
